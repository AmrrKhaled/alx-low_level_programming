#include "main.h"
#include <stdio.h>

/**
 * mul-function  check the two inputs and return the addition
 *
 * @x: check the input of function
 * @y: check the second input
 * Return: retuns 1 if c is lower otherwise alwayzi 0 (success)
*/
int mul(int x, int y)
{
	return (x * y);
}
